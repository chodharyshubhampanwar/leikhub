{
  /* <FeatureSection>
        <HeadingHero style={{ padding: "20px 0" }}>
          {data.subHeader.title}
        </HeadingHero>
        {data.features.map((feature, index) => (
          <FlexItem key={index}>
            <div style={{ flex: 2, display: "flex", flexDirection: "column" }}>
              <HeadingHero>{feature.title}</HeadingHero>
              <p>{feature.description}</p>
            </div>
            <div style={{ flex: 1, display: "flex", flexDirection: "column" }}>
              <StyledImage src={feature.image} alt="description" />
            </div>
          </FlexItem>
        ))}
      </FeatureSection> */
}
